import { makeStyles } from "@material-ui/core";

export const useAppRouterStyles = makeStyles(() => ({
  loaderContainer: {
    alignItems: "center",
    display: "flex",
    height: "100%",
    justifyContent: "center",
    width: "100%",
  },
}));
